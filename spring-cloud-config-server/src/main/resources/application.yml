spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pateluday07/spring-cloud-config-repo.git
          default-label: master

server:
    port: 8888
